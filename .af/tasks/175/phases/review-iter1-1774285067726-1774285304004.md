# Phase: review iter 1 — converged

## review Output
{
  "verdict": "pass",
  "dimensions": [
    {
      "name": "Requirements Met",
      "score": 9,
      "gaps_to_10": "The home-screen greeting renders '你好, 小明! / Hello, 小明!' (bilingual) rather than the bare '你好, 小明!' stated in requirement 1, which is strictly a superset of the requirement and not a defect. All 10 requirements are otherwise implemented: onboarding wizard with IndexedDB persistence, ExploreCards with audio, iOS AudioContext unlock in StepWelcome 'Get Started' handler, CelebrationOverlay with confetti, 'Go Home' navigation back to home, ≥88px touch targets throughout, image error fallback, bilingual error.tsx, and no API keys in src/."
    },
    {
      "name": "Error Handling",
      "score": 8,
      "gaps_to_10": "All DB calls are wrapped in try/catch. Audio errors are non-fatal with graceful AudioButton error state. Image 404s use onError fallback. ExploreCards has loading and empty-state renders. useDB silently returns null on failure without surfacing an error UI to the user — callers handle null db by returning early, which means the app silently does nothing if IndexedDB is unavailable, rather than showing an error message. A user-facing error toast or message when DB fails to open would close this gap."
    },
    {
      "name": "UX Completeness",
      "score": 8,
      "gaps_to_10": "Loading spinners are present in root page and ExploreCards. All buttons are functional. The 'start-session-btn' links to /session which only renders a placeholder stub ('Coming soon'), so tapping Start Session leads to a dead end — this is a P2 cosmetic issue as no navigation is broken (user can tap home button). After celebration, 'Go Home' correctly navigates to / via router.push. The daily word count on the home screen updates only after a page refresh (useEffect runs on mount, not on focus), so a returning user may see a stale count until refresh."
    },
    {
      "name": "Code Quality",
      "score": 9,
      "gaps_to_10": "All component files are under 200 lines. Business logic is properly separated into lib/ and hooks/. next.config.ts uses output: 'standalone' instead of the plan-specified output: 'export' — the build succeeds either way and all pages are statically rendered, but standalone is a minor deviation that removes the build-time guard against accidentally using SSR features. audio.ts imports Howl from 'howler' at module top level without a typeof-window guard; this works because all importers have 'use client', but is a latent fragility if audio.ts is ever imported in a server context."
    },
    {
      "name": "Test Coverage",
      "score": 8,
      "gaps_to_10": "35 tests pass across 6 test files. All required test cases from the plan are implemented. PinPad tests produce act() warnings (state updates from setTimeout not wrapped in act) — warnings do not cause failures but indicate test hygiene issues. The crypto.test.ts uses a mocked digest that always returns constant bytes, so the 'same input → same hash' and 'different input → different hash' cases are not truly exercised against a real SHA-256 implementation. No integration tests for the full onboarding → home flow."
    },
    {
      "name": "Security",
      "score": 10,
      "gaps_to_10": "No API keys or secrets found in any src/ file. PIN is hashed with SHA-256 + static salt using Web Crypto API before storage. User name input is trimmed but not further sanitized — acceptable for a local-only kids app with no server. Salt is hardcoded ('littlebridge-2026') which is a known weakness but acceptable for a PIN-based parental gate with no server-side validation."
    }
  ],
  "average_score": 8.67,
  "issues": [
    {
      "severity": "P2",
      "file": "next.config.ts",
      "description": "output is set to 'standalone' instead of 'export' as specified in the plan. The plan chose 'export' to prevent accidental use of SSR features (IndexedDB, Howler, canvas-confetti all require window). With 'standalone', SSR features could silently be used without a build-time error. Build currently passes and all pages render as static, so there is no runtime failure.",
      "fix_guidance": "The correct approach is to change output: 'standalone' to output: 'export' in next.config.ts. This enforces the static-only constraint at build time and aligns with the plan's risk mitigation strategy.",
      "checklist_item": "architecture"
    },
    {
      "severity": "P2",
      "file": "src/lib/audio.ts",
      "description": "Howler is imported at the module top level (import { Howl } from 'howler') without a typeof window !== 'undefined' guard. Howler accesses window on import. This is currently safe because all importers have 'use client', but if audio.ts is ever imported from a server component or utility, the build will fail with a 'window is not defined' error.",
      "fix_guidance": "The correct approach is to add a typeof window guard or use dynamic import inside the AudioManager constructor: 'if (typeof window === \"undefined\") return;' at the top of getOrCreate(). Alternatively, mark audio.ts with 'use client' at the top to make the constraint explicit.",
      "checklist_item": "architecture"
    },
    {
      "severity": "P2",
      "file": "src/components/HomeScreen.tsx",
      "description": "The daily word count (todayWords) is loaded once on mount and is not refreshed when the user returns from an activity. After completing ExploreCards and tapping 'Go Home', the home screen remounts which triggers the useEffect — this works correctly on navigation. However if the component stays mounted (e.g. future SPA transitions without remount), the count would be stale.",
      "fix_guidance": "The correct approach is to use a router event listener or React Query-style revalidation on focus to refresh the session count. For the current implementation this is not a user-visible bug since router.push('/') causes remount.",
      "checklist_item": "ux"
    },
    {
      "severity": "P2",
      "file": "src/__tests__/PinPad.test.tsx",
      "description": "PinPad tests produce React act() warnings for state updates triggered by the 100ms setTimeout in handleKey. The tests use 'await new Promise(r => setTimeout(r, 200))' instead of 'await act(async () => { await new Promise(r => setTimeout(r, 200)); })'. Tests still pass but the warnings indicate the tests are not properly simulating React's update batching.",
      "fix_guidance": "The correct approach is to wrap the setTimeout-based assertions in act(): 'await act(async () => { await new Promise(r => setTimeout(r, 200)); }); expect(onSubmit).toHaveBeenCalledWith(\"1234\");'",
      "checklist_item": "test_quality"
    },
    {
      "severity": "P2",
      "file": "src/app/session/page.tsx",
      "description": "The 'Start Session' button on the home screen (data-testid start-session-btn) links to /session, which only renders a 'Coming soon' placeholder stub. Tapping this button leads to a dead-end page with no content.",
      "fix_guidance": "The correct approach is either to implement a basic session page that launches a guided activity, or to change the start-session-btn href to link to /activities/explore-cards until the session flow is built. The current stub gives a poor UX for the primary CTA on the home screen.",
      "checklist_item": "ux"
    }
  ],
  "summary": "LittleBridge passes production-readiness review with an average score of 8.67/10. All 10 functional requirements are implemented: the onboarding wizard correctly persists settings to IndexedDB, page refresh shows the home screen (not onboarding), ExploreCards displays vocabulary with audio, iOS AudioContext is unlocked on the 'Get Started' tap, CelebrationOverlay fires confetti with child name and word count, 'Go Home' returns to the updated home screen, all touch targets are ≥88px, image 404s gracefully fall back to placeholder.png, error.tsx provides a bilingual error screen, and no API keys appear in src/. All 35 tests pass. The five P2 issues are: (1) next.config.ts uses 'standalone' instead of the plan-specified 'export' output mode, (2) Howler is imported at module top level without a window guard, (3) minor UX stale-count concern, (4) act() warnings in PinPad tests, and (5) the 'Start Session' button leads to a placeholder stub page. None of these cause user-visible crashes or broken flows in the current implementation."
}

